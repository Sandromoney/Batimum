"use client";

/**
 * Ancien bouton Assistant Batimum — désactivé (MUM IA uniquement).
 * Conservé pour ne pas casser les imports existants.
 */
export function MumIaContextButton(_props: {
  source?: string;
  entityId?: string;
  entityLabel?: string;
  description?: string;
  typeChantier?: string;
  suggestedPrompts?: string[];
  returnHref?: string;
  extra?: Record<string, unknown>;
  className?: string;
  size?: "sm" | "md";
}) {
  return null;
}
